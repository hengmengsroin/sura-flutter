name: sura_flutter
description: A flutter package for custom widgets and utilities function.
version: 1.4.0
homepage: https://github.com/asurraa/sura_flutter

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  universal_platform: ^1.0.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
